# Run the tool with default settings (from project root)
run:
	@echo "Running classmapper..."
	cd .. && ./tools/classmapper/classmapper

# Run the tool with custom settings (from project root)
run-custom:
	@echo "Running classmapper with custom settings..."
	cd .. && ./tools/classmapper/classmapper $(ARGS)